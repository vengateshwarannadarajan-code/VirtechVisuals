# GitHub Pages deployment disabled — site now deployed via Vercel
# Vercel auto-deploys on every push to master
name: Disabled - Using Vercel

on:
  workflow_dispatch:

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Deployments handled by Vercel"
